# Configuration pour les tests

# Base de données de test (SQLite en mémoire)
DB_DIALECT=sqlite
DB_STORAGE=:memory:

# JWT
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Email (mock)
SENDGRID_API_KEY=test-key
SENDGRID_VERIFIED_SENDER=test@example.com

# Google AI (mock)
GOOGLE_AI_API_KEY=test-gemini-key

# Serveur
PORT=3001
NODE_ENV=test
